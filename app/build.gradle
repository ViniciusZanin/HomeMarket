apply plugin: 'com.android.application'

android {
    signingConfigs {
        KleinZanin {
            keyAlias 'KZ'
            keyPassword 'kleinzanin'
            storeFile file('C:/Users/Felippe/HomeMarket/KleinZanin.jks')
            storePassword 'kleinzanin'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.ufabc.kleinzanin.homemarket"
        minSdkVersion 15
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.KleinZanin
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.KleinZanin
        }
        debug {
            signingConfig signingConfigs.KleinZanin
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.google.android.gms:play-services:6.5.87'
}
